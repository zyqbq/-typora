```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #box {
            width: 200px;
            height: 200px;
            background-color: red;
        }
    </style>
</head>

<body style="height:1200px">
    <div id="box"></div>
    <script>
        const dug = $el => {
            const startPosition = {};
            const movePosition = {};
            const curPosition = {
                x: 0,
                y: 0
            };
            $el.addEventListener('touchstart', startHandler, false);
            $el.addEventListener('touchmove', moveHandler, false);
            $el.addEventListener('touchend', endHandler, false);
            $el.addEventListener('touchcancel', endHandler, false);
            function startHandler(evt) {
                evt.preventDefault();
                const touch = evt.changedTouches[0];
                startPosition.x = touch.pageX;
                startPosition.y = touch.pageY;
            };
            function moveHandler(evt) {
                const touch = evt.changedTouches[0];
                movePosition.x = curPosition.x + touch.pageX - startPosition.x;
                movePosition.y = curPosition.y + touch.pageY - startPosition.y;
                $el.style.transform = `translate3d(${movePosition.x}px,${movePosition.y}px,0)`;
            };
            function endHandler(evt) {
                curPosition.x = movePosition.x;
                curPosition.y = movePosition.y
            };
        }
        dug(document.getElementById('box'))
    </script>
</body>

</html>
```

# pointer版

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #box {
            width: 200px;
            height: 200px;
            background-color: red;
        }
    </style>
</head>

<body style="height:1200px">
    <div id="box"></div>
    <script>
        const dug = $el => {
            const startPosition = {};
            const movePosition = {};
            const curPosition = {
                x: 0,
                y: 0
            };
            $el.addEventListener('pointerdown', startHandler, false);
            $el.addEventListener('touchstart', evt => {
                evt.preventDefault()
            }, false);
            function startHandler(evt) {
                // evt.preventDefault();
                startPosition.x = evt.pageX;
                startPosition.y = evt.pageY;
                document.addEventListener('pointermove', moveHandler, false);
                document.addEventListener('pointerup', endHandler, false);
                document.addEventListener('pointercancel', endHandler, false);
            };
            function moveHandler(evt) {
                movePosition.x = curPosition.x + evt.pageX - startPosition.x;
                movePosition.y = curPosition.y + evt.pageY - startPosition.y;
                $el.style.transform = `translate3d(${movePosition.x}px,${movePosition.y}px,0)`;
            };
            function endHandler() {
                curPosition.x = movePosition.x;
                curPosition.y = movePosition.y
                document.removeEventListener('pointermove', moveHandler, false);
                document.removeEventListener('pointerup', endHandler, false);
                document.removeEventListener('pointercancel', endHandler, false);
            };
        }
        dug(document.getElementById('box'))
    </script>
</body>

</html>
```

